<!--pages/list/list.wxml-->
<view class="container">
  <view class='list-group'>
    <view class="list" wx:for="{{allName}}" wx:key="{{index}}" data-idx="{{index}}" bindtap="toChoice">
      <text>帮你选择{{index}}:</text>
      <text>{{item}}</text>
    </view>
  </view>
</view>